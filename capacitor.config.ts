import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'com.laserando.orders',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
